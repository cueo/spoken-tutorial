Welcome spoken tutorial Function calls C C++. In tutorial, learn type function calls namely- * call value * call reference. We example. To record tutorial, I using Ubuntu Operating system version 11.10, gcc g++ Compiler version 4.6.1. Let us start introduction function call value. It method passing arguments function. When pass variable value, makes copy variable passing function. Changes made arguments, inside function, remain function. It affected outside function. Let us see program 'function call value'. I already typed program editor. I open it. Please note file name callbyval.c. In program, calculate cube number. Let explain code now. This header file. Here function cube argument int x. . In function, calculate cube x return value x. This main() function. Here give value n 8, 'n' integer variable. Then call function cube. And print value 'n' cube n. And return statement. Now let us execute program. open terminal window pressing Ctrl, Alt T keys simultaneously keyboard. To compile , type gcc space callbyval.c space hyphen o space val. Press Enter. Now, type ./val (dot slash val). Press Enter. The output displayed as: Cube 8 512. Now see 'function call reference'. Let us go back slides. It another method passing arguments function. This method copies address argument instead value. Changes made arguments, inside function, affect outside. In need declare arguments pointer type. Let us see example function call reference. Note file name callbyref.c. This header file stdio.h. Then function swap. This function exchange values variables. Value 'a' stored value 'b' vice versa. You see arguments passed function pointer type. Here declared integer variable t. First, value 'a' stored 't'. Then value 'b' stored 'a'. And value 't' stored 'b'. Like this, values exchanged. This main() function. Here declared two integer variables j. Then take values j user inputs. Ampersand (&i) Ampersand j (&j) give memory address j. First, print values swapping. Then call function swap. And print values swapping. And return statement. Now let us execute program. Switch back terminal. To compile type gcc space callbyref dot c space hyphen o space ref. Press Enter. Now type dot slash ref. Press Enter. We see, 'Enter values'. I enter 6 4. The output displayed as: Before swapping 6 4 After swapping 4 6. Now let us see execute program C++. I code, lets go it. This second program, function call reference . Note file name callbyref.cpp Let us go code. This header file iostream . Here using std namespace. The function declaration C++. In pass arguments ampersand x ampersand y. This give memory address x y. Then swap values. Rest code similar C code. The printf statement replaced cout scanf statement replaced cin. Now let us execute program. Come back terminal. To compile type, g++ space callbyref.cpp space hyphen o space ref1, press Enter. Now, type dot slash ref1. Press Enter. Here displayed as: Enter values b I enter 4 3. The output displayed. Before swapping b 4 3 After swapping b 3 4 This brings us end tutorial. Let us go back slides. Let us summarize, In tutorial learnt: Function call value. And Function call reference. As assignment, write similar program calculate cube number, using call value C++. Watch video available link shown below. It summarizes Spoken Tutorial project. If good bandwidth, download watch it. The Spoken Tutorial Project Team: Conducts workshops using spoken tutorials. Gives certificates pass online test. For details, please write contact@spoken-tutorial.org. Spoken Tutorial Project part Talk Teacher project. It supported National Mission Education ICT, MHRD, Government India. More information mission available link shown below. This Ashwini Patil IIT Bombay, signing off. Thank You joining.