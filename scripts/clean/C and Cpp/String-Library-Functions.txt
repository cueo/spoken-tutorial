Welcome spoken-tutorial String Library Functions C. In tutorial learn String Library Functions. We help examples. To record tutorial, I using Ubuntu Operating System version 11.10, gcc Compiler Version 4.6.1. Let us start introduction string library functions. These group functions implementing operations strings. Various operations copying, concatenation, searching etc supported. Let us see string library functions. Here strncpy function. The syntax strncpy(char str1, char str2, int n ); It copies first n characters string str2 string str1. example, char strncpy( char hello, char world, 2); The output Wollo . Here Wo string 2 rest characters string 1. Now see strncmp function, syntax strncmp(char str1, char str2, int n); It compare first n characters string 2 string 1. example int strncmp(char ice, char icecream, 2); The output 0. Now see use string library functions. I going show commonly used string functions. I already typed program editor, I open it. Here, string length function. Note file name strlen.c. In this, find length string. These header files stdio.h string.h. This main() function. Here character variable 'arr'. It stores value 'Ashwini' . Then integer variable len1 . Here find length string using strlen function. The result stored 'len1'. Then print string length string. And return statement. Now let us execute program. Open terminal window pressing Ctrl, Alt T keys simultaneously keyboard. To compile, type: "gcc" space "strlen.c" space “-o” space “str1”. Press Enter. Type (dot slash) ./str1. Press Enter. The output displayed string = Ashwini, Length = 7 You count here. 1,2,3,4,5,6, 7. Let us see another string function. Here string copy function. Note file name strcpy.c . In this, copy source string target string. Here 'Ice' source string, copied target string. This strcpy function Here print source string target string. Let us execute see. Come back terminal. To compile, type gcc space strcpy.c space hyphen o space str2. Press Enter Type (dot slash)./str2 . Press Enter . The output displayed as: source string = Ice target string = Ice Now let us see another string function. Now see string compare function. Note file name strcmp.c. In this, compare two strings. Here character variables str1 str2 . str1 stores value 'Ice' str2 stores value 'Cream'. Here integer variables j. In compare string using strcmp function. Here compare str1 i.e 'Ice' 'Hello'. The result stored i. In compare string2 i.e 'Cream' 'Cream' The result stored j. Then print results. And return statement. Let us execute program. Come back terminal. To compile type gcc space strcmp.c space hyphen o space str3 Press Enter. Type (dot slash) ./str3 The output displayed 1, 0. Come back program. Here get 1 get 0. Let us come back slides. Let us summarize. In tutorial learned, String library functions- * strlen() * strcpy() * strcmp() * strncpy() *and strncmp(). As assignemnt, write C Program concatenate String 'best' String 'bus'. Hint: strcat(char str1, char str2); Also explore functions string library. Watch video available link shown below. It summarizes Spoken Tutorial project. If good bandwidth, download watch it. The Spoken Tutorial Project Team: Conducts workshops using spoken tutorials. Gives certificates pass online test. For details, please write to, contact@spoken-tutorial.org. Spoken Tutorial Project part Talk Teacher project. It supported National Mission Education ICT, MHRD, Government India. More information mission available link shown below. This Ashwini Patil IIT Bombay, signing off. Thank joining.