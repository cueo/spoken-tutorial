Welcome spoken tutorial Increment Decrement Operators C C++. In tutorial, learn about: Increment decrement operators. '++' eg. a++ postfix increment operator. ++a prefix increment operator. '--' eg. a-- postfix decrement operator. --a prefix decrement operator. We also learn Typecasting. To record tutorial, I using: Ubuntu 11.10 operating system, gcc g++ Compiler version 4.6.1 Ubuntu. The ++ operator increases existing value operand one. a++ ++a equivalent = + 1. The -- operator decreases existing value operand one. a-- --a equivalent = - 1. I demonstrate use increment decrement operators help C program. I already made program, I'll explain code. Here, code increment decrement operators C. Here, I taken integer variable holds value 1. This way able observe changes value a. It thus give us better idea working operators. Let's see postfix increment operator works. The output printf statement 1. The value won't change. This postfix operation occurs operand evaluated. If operation performed a++, performed current value a. After value incremented. Now see value here, incremented 1. We initialize 'a' 1 reflect changes. We come prefix increment operators. This printf statement prints 2 screen. This prefix operation occurs operand evaluated. So value 'a' first incremented 1 printed. We print a's value see changes. Now let's check executing code. I comment following lines. Type /*, */ Click Save. I saved file incrdecr.c. Open terminal window pressing Ctrl, Alt T keys simultaneously. To compile, type following terminal; gcc space incrdecr dot c space minus o space incr. Press Enter. To execute code, type ./incr (dot slash incr). Press enter. The output displayed screen. This output print a++. This output print ++a. We see result discussed before. Now Coming back rest program. I explain postfix prefix decrement operators. Remove multi-line comments . We assign value 1 a. This printf statement outputs value 1 explained previously. a's value decremented a-- evaluated postfix expression. The next statement prints a's value o. a's value decremented 1. We prefix decrement operator. Output printf statement would 0. As prefix operation. The prefix operation occurs operand evaluated. This printf statements output 0. No changes made a's value. Type return 0; close ending curly bracket. Click Save. Switch back terminal. To compile, type following terminal; gcc space incrdecr dot c space minus o space incr. Press Enter. To execute, type ./incr. Press Enter. This output print a--. This output print --a. So, see increment decrement operator work. If want write program C++, I make changes C code. Let go back editor. Here C++ file necessary code. Notice header different C file header. We using namespace statement also. Also note output statement C++ cout. So, apart differences, two codes similar. Save file. The file saved extension .cpp. Let's compile code. Open terminal type g++ space incrdecr dot cpp space minus o space incr. Press Enter. To execute, type ./ incr (dot slash incr). Press Enter. The output displayed screen. So, see output identical C program. We concept typecasting. It implemented way C C++. Typecasting used make variable one type, act like another type. Typecasting done enclosing data type, want, within parentheses. This cast put front variable want cast. This typecast valid one single operation only. Now a' behave float variable, single operation. Here example I already created. I shall explain code. We first declare variables b integers c float. assigned value 5. b assigned value 2. We perform operations b. We divide b. The result division stored c. We used %.2f denote precision 2 decimal places. The result displayed 2.00 expected result 2.50. The fractional part truncated operands b integers. To perform real division, one operands typecast float. Here typecasting float. c holds value real division. Now result real division displayed. The answer 2.50 expected. Type return 0; close ending curly bracket. Click Save. Save file .c (dot c) extension. I saved file typecast.c. Open terminal. To compile, type gcc space typecast dot c space minus o space type. Press Enter. To execute, type ./type. Press Enter. The output displayed screen. Looking two values see effects typecasting. We summarize tutorial now. In tutorial learnt- use increment decrement operators, learnt forms, Postfix Prefix, also learnt typecasting used. As assignment: Write program solve following expression, divided b plus c divided d. The values a, b, c d taken input user. Use typecasting perform real division. Watch video available following link. It summarizes Spoken Tutorial project. If good bandwidth, download watch it. The Spoken Tutorial Project Team: Conducts workshops using spoken tutorials. Gives certificates pass online test. For details, please write contact spoken hyphen tutorial dot org. Spoken Tutorial Project part Talk Teacher project. It supported National Mission Education ICT, MHRD, Government India. More information mission available spoken hyphen tutorial dot org slash NMEICT hyphen Intro. This Ritwik Joshi IIT Bombay. Thank joining.